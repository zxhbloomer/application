spring:
  cloud:
    config:
      label: master
      uri: http://server1:9200  #ConfigServer地址
      name: config-client-dev    #方式1:通过name指定文件(yml和properties都可以读取到)
#      profile: dev               #方式2:通过profile指定文件,[applicationName + profile].[yml || properties] == PS:建议把config的配置放在bootstrap里面,不然可能无法读取到!!!
  boot:
    admin:
      client:
        url: http://server1:9400
  zipkin:
    base-url: http://server1:9411/
    sender:
      type: web #把数据发送到WEB端
    locator:
      discovery:
        enabled: true # 支持通过服务发现来定位host name：
  sleuth:
    web:
      client:
        enabled: true #链路配置
    sampler:
      probability: 1.0  # 将采样比例设置为 1.0，也就是全部都需要。默认是 0.1
eureka:
  instance:
    instance-id: ${spring.application.name}:${server.port}
  client:
    security:
      basic:
        user: admin
        password: 123
    serviceUrl:
      defaultZone: https://${eureka.client.security.basic.user}:${eureka.client.security.basic.password}@server1:9100/eureka/